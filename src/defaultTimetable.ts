/**
 * Default timetable is loaded from the `public/` folder (URL below).
 *
 * Important: only files inside `public/` are served by the dev server and copied to `dist/`.
 * If you edit `Substitution_System.xlsx` in the project root, copy it to
 * `public/Substitution_System.xlsx` (or use “Choose file” in the app to load your copy once).
 */
export const DEFAULT_TIMETABLE_URL = '/Substitution_System.xlsx'
